// Quotation detail / pricing screen — built in Phase 4
export default function QuotationDetailPage({ params }: { params: { no: string } }) {
  return <div>Quotation #{params.no} — coming in Phase 4</div>;
}
